# OURFW v0.7.1 — hardware verification log (2026-08-17)

> Notes only. The branch `agent/v0.7.1-hardware-fixes-notes` intentionally does not implement v0.7.1 source fixes.

## Live runtime baseline

Physical device: TP-Link EC220-G5 v2, kernel `3.4.113`, base mutable `v0.6.3`, overlay `v0.7.0-noflash-v063`.

## HWFIX1 applied successfully

Live mutable HWFIX1 was applied without flashing firmware, restarting the Hysteria engine, or enabling routing.

Post-install hardware checks confirmed:

- text status reports `OURFW_VERSION=v0.7.0-noflash-v063`;
- JSON status reports `version=v0.7.0-noflash-v063`;
- subscription primary = `n340e06656f3cf3e8`;
- subscription backup = `n8a330d78bb61f243`;
- `SUBSCRIPTION_LABEL_MAX_BYTES=160` remained intact;
- all modified shell files passed `sh -n`;
- `HY2_ENGINE=running`;
- `HY2_ROUTE=off`;
- `NFNETLINK/IP_SET/IP_SET_HASH_NET/XT_SET=loaded`;
- reconnect cooldown was `0` for the healthy connection;
- policy routing was clean and `OURFW_HY2` absent.

Pre-HWFIX1 backup:

```text
/tmp/OURFW-v070-before-HWFIX1.tar.bz2
SHA256=aa2c2c110c5321efb163086afc1b783eb2e826ca0fe37366a8d003c14f9ac52e
```

## Smart ipset autoload fix — hardware confirmed

To prove the patched Smart path did not merely reuse manually loaded modules:

1. empty OURFW ipsets were destroyed;
2. `xt_set`, `ip_set_hash_net`, `ip_set`, and `nfnetlink` were unloaded in dependency order;
3. `/proc/modules` confirmed the full chain absent (`IPSET_MODULE_CHAIN=ABSENT_OK`);
4. one `hy2-arm-smart` call was executed with an empty VPN set;
5. arm returned `rc=0` and Smart automatically reloaded all four modules;
6. route state briefly contained pref `10010`, table `101` local route and `OURFW_HY2`;
7. `hy2-route-off` was called immediately;
8. final state returned to local/main/default only, empty table `101`, and `OURFW_HY2=ABSENT_OK`.

Observed module state after automatic reload:

```text
xt_set ... Live
ip_set_hash_net ... Live
ip_set ... xt_set,ip_set_hash_net, Live
nfnetlink ... ip_set, Live
```

This closes hardware verification for HW-07-01.

## HWFIX1b — Storage hash-directory hardening confirmed

A second tiny live hotfix changed only the OURFW `save_storage()` wrapper to ensure `/tmp/hashes` exists before invoking Padavan `mtd_storage.sh save`.

Hardware result:

```text
Save storage files to MTD partition "/dev/mtdblock3"
Unlocking 'Storage' ...
Writing from '/tmp/storage.tar.bz2' to MTD 'Storage' ...  [ok]
Done.
```

The previous warning about inability to create `/tmp/hashes/storage_md5` did not recur.

The live wrapper now contains:

```sh
save_storage() {
    mkdir -p /tmp/hashes 2>/dev/null || true
    /sbin/mtd_storage.sh save >/tmp/ourfw-storage-save.log 2>&1
}
```

Volatile cache confirmed present:

```text
/tmp/hashes
/tmp/hashes/storage_md5
```

HWFIX1b marker:

```text
v0.7.0-noflash-v063-live-hwfix1b
```

Pre-HWFIX1b backup:

```text
/tmp/OURFW-v070-HWFIX1-before-HWFIX1B.tar.bz2
SHA256=6beb75647376e5da58e8fa04d36f4f5ffa1106ced3235c28da326a445d3e2457
```

Post-HWFIX1b safety state remained:

- `HY2_ENGINE=running`;
- `HY2_ROUTE=off`;
- `ip rule` = local/main/default only;
- `OURFW_HY2=ABSENT_OK`;
- no Hysteria restart;
- no routing change.

## Remaining live verification

- reconnect cooldown/backoff under an isolated failure probe;
- sanitized Hysteria startup failure classification from the installed VPN API logic;
- optional separate TCP TPROXY acceptance test later;
- eventual reboot/persistence test only after the live session is otherwise complete.
