# OURFW v0.7.1 — live HWFIX1 follow-up

> Notes only. No v0.7.1 source code is implemented in this branch.

## Live router hotfix result

A separate mutable-runtime live hotfix was applied to the physical EC220-G5 v2 running base `v0.6.3` with overlay `v0.7.0-noflash-v063`.

Post-install hardware audit confirmed:

- `OURFW_VERSION=v0.7.0-noflash-v063` in text status;
- JSON/WebUI status reports `v0.7.0-noflash-v063`;
- subscription primary remains `n340e06656f3cf3e8`;
- subscription backup remains `n8a330d78bb61f243`;
- `SUBSCRIPTION_LABEL_MAX_BYTES=160` remains intact;
- all five modified shell files pass `sh -n`;
- Hysteria engine remained running;
- Hysteria routing remained off;
- `nfnetlink`, `ip_set`, `ip_set_hash_net`, and `xt_set` report loaded;
- reconnect cooldown reports `0` after a healthy running connection;
- `ip rule` is clean (`local/main/default` only);
- table 101 is empty;
- `OURFW_HY2` is absent.

The live backup created before applying the hotfix was:

```text
/tmp/OURFW-v070-before-HWFIX1.tar.bz2
SHA256=aa2c2c110c5321efb163086afc1b783eb2e826ca0fe37366a8d003c14f9ac52e
```

The backup can contain private configuration/subscription data and must not be published.

## HW-07-09 — Padavan `mtd_storage.sh save` assumes `/tmp/hashes` exists

### Hardware symptom

During the successful live hotfix Storage save, Padavan printed:

```text
/sbin/mtd_storage.sh: line 89: can't create /tmp/hashes/storage_md5: nonexistent directory
```

but then continued with:

```text
Unlocking 'Storage' ...
Writing from '/tmp/storage.tar.bz2' to MTD 'Storage' ...  [ok]
Done.
```

Post-install audit confirmed `/tmp/hashes` did not exist.

### Relevant Padavan logic

`func_save()` performs roughly this order:

1. build the temporary Storage archive input;
2. run `md5sum -c -s $hsh` as a write-skip optimization;
3. if different, write `md5sum $tmp > $hsh`;
4. compress the archive;
5. validate compressed size;
6. call `mtd_write write ... Storage`.

On this hardware/runtime, the missing parent directory prevents only the volatile MD5 cache file from being created. The actual MTD write still completed successfully.

### v0.7.1 hardening

OURFW should not modify the vendor `/sbin/mtd_storage.sh`, but every OURFW-controlled call to Storage save should defensively ensure the volatile parent exists first:

```sh
mkdir -p /tmp/hashes 2>/dev/null || true
/sbin/mtd_storage.sh save
```

This avoids noisy errors and restores the intended unchanged-content write-skip behavior without patching the read-only Padavan script.

Add a regression/static check so OURFW save wrappers use the defensive helper rather than calling `mtd_storage.sh save` directly from multiple places.

## Remaining live verification before closing the hardware session

- unload only the ipset kernel dependency chain after destroying unused sets;
- verify the modules are genuinely absent;
- call the patched Smart arm path with an empty VPN set;
- verify Smart itself automatically reloads `nfnetlink -> ip_set -> ip_set_hash_net -> xt_set`;
- immediately call route-off (do not confirm) and verify clean routing state;
- later, separately exercise reconnect backoff/failure-detail behavior under controlled conditions.
