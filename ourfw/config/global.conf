# OURFW global settings. key=value only.
OURFW_ENABLED=1
ROLLBACK_TIMEOUT=90
LOG_LEVEL=1

# Default behavior: safe direct Internet unless explicit smart-routing rule matches.
ROUTING_MODE=smart
KILLSWITCH=1

# Keep automatic component history small because EC220 Storage is tiny.
HISTORY_KEEP=2
